<UserControl x:Class="FortniteSwapper.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="760">
            <TextBlock Text="Welcome to Fortnite Swapper" Style="{StaticResource PageTitle}" />

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding Status}" FontSize="15" Foreground="{StaticResource TextBrush}" />
                    <TextBlock Margin="0,6,0,0" Style="{StaticResource MutedText}">
                        <Run Text="Install:" /><Run Text=" " /><Run Text="{Binding FortnitePath}" />
                    </TextBlock>
                    <TextBlock Margin="0,2,0,0" Style="{StaticResource MutedText}">
                        <Run Text="Version:" /><Run Text=" " /><Run Text="{Binding Version}" />
                    </TextBlock>
                    <TextBlock Margin="0,2,0,0" Style="{StaticResource MutedText}">
                        <Run Text="Mapping coverage:" /><Run Text=" " /><Run Text="{Binding MappingCoverage}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Quick actions" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" />
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Browse cosmetics" Command="{Binding GoToBrowseCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" />
                        <Button Content="Go to Swap" Command="{Binding GoToSwapCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" />
                        <Button Content="Settings" Command="{Binding GoToSettingsCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                    <Button Content="Restore all swaps" Command="{Binding RestoreAllCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,10,0,0" HorizontalAlignment="Left" />
                    <TextBlock Margin="0,8,0,0" Foreground="{StaticResource SuccessBrush}" Text="{Binding RestoreMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource DisclaimerStyle}">
                <StackPanel>
                    <TextBlock Text="Important" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" Margin="0,0,0,6" />
                    <TextBlock Style="{StaticResource MutedText}" TextWrapping="Wrap">
                        This tool changes Fortnite's local game files on your PC only. Other players still see your real cosmetics.
                        Modifying game files can violate Epic's Terms of Service and may be detected by anti-cheat software.
                        Use at your own risk, keep backups, and prefer a separate account. See Settings for full details.
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
