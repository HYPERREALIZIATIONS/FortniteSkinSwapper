<UserControl x:Class="FortniteSwapper.Views.SwapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FortniteSwapper.Models">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Swap" Style="{StaticResource PageTitle}" />

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource MutedText}" Text="{Binding VersionsNote}" TextWrapping="Wrap" />
                <TextBlock Margin="0,6,0,0" Foreground="{StaticResource SuccessBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="1. Source — a cosmetic you own" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding OwnedCosmetics}" SelectedItem="{Binding Source, Mode=TwoWay}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:Cosmetic}">
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <Image Source="{Binding IconUrl}" Width="34" Height="34" Stretch="Uniform" />
                                    <StackPanel Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" />
                                        <TextBlock Style="{StaticResource MutedText}" Text="{Binding Type}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="12,0,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="2. Target — the look to swap in" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding Catalog}" SelectedItem="{Binding Target, Mode=TwoWay}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:Cosmetic}">
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <Image Source="{Binding IconUrl}" Width="34" Height="34" Stretch="Uniform" />
                                    <StackPanel Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" />
                                        <TextBlock Style="{StaticResource MutedText}" Text="{Binding Type}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8">
                    <Run Text="3. Apply:" />
                    <Run Text="{Binding Source.Name, Mode=OneWay}" />
                    <Run Text="  →  " />
                    <Run Text="{Binding Target.Name, Mode=OneWay}" />
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Apply swap" Command="{Binding ApplyCommand}" Style="{StaticResource PrimaryButtonStyle}"
                            IsEnabled="{Binding CanApply}" />
                    <Button Content="Restore all" Command="{Binding RestoreAllCommand}" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="10,0,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Expander Grid.Row="4" Header="Activity log" IsExpanded="True" Margin="0,0,0,12"
                  Foreground="{StaticResource TextBrush}">
            <ListBox ItemsSource="{Binding LogLines}" Height="120" Background="{StaticResource PanelBrush}"
                     Foreground="{StaticResource MutedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" />
        </Expander>

        <Border Grid.Row="5" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Applied swaps (restore any time)" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8" />
                <ItemsControl ItemsSource="{Binding AppliedSwaps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:SwapRecord}">
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock VerticalAlignment="Center" Style="{StaticResource MutedText}">
                                    <Run Text="{Binding SourceName}" />
                                    <Run Text=" → " />
                                    <Run Text="{Binding TargetName}" />
                                    <Run Text=" (" />
                                    <Run Text="{Binding AppliedAt}" />
                                    <Run Text=")" />
                                </TextBlock>
                                <Button Grid.Column="1" Content="Restore" Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Style="{StaticResource SecondaryButtonStyle}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
