<UserControl x:Class="FortniteSwapper.Views.BrowseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:FortniteSwapper.Converters"
             xmlns:models="clr-namespace:FortniteSwapper.Models">

    <UserControl.Resources>
        <conv:LockerLabelConverter x:Key="LockerLabel"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Browse Cosmetics" Style="{StaticResource PageTitle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Width="280" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}" />
            <ComboBox Width="140" Margin="10,0,0,0" ItemsSource="{Binding TypeFilters}"
                      SelectedItem="{Binding TypeFilter}" Background="{StaticResource PanelBrush}"
                      Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource BorderBrush}" />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Style="{StaticResource MutedText}">
                <Run Text="{Binding StatusMessage}" />
            </TextBlock>
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Style="{StaticResource MutedText}">
                <Run Text="In locker:" /><Run Text=" " /><Run Text="{Binding OwnedCount}" />
            </TextBlock>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Filtered}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Cosmetic}">
                        <Border Width="160" Margin="6" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <Border Height="140" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="8">
                                    <Image Source="{Binding IconUrl}" Stretch="Uniform" />
                                </Border>
                                <TextBlock Margin="0,8,0,0" Text="{Binding Name}" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" Height="38"
                                           VerticalAlignment="Top" />
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                                    <Rectangle Width="10" Height="10" RadiusX="2" RadiusY="2" Fill="{Binding Rarity, Converter={StaticResource RarityToBrush}}" />
                                    <TextBlock Margin="6,0,0,0" Style="{StaticResource MutedText}" Text="{Binding Type}" />
                                </StackPanel>
                                <Button Command="{Binding DataContext.ToggleOwnedCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                            <Setter Property="Content" Value="Add to My Locker" />
                                        </Style>
                                    </Button.Style>
                                    <Button.Content>
                                        <MultiBinding Converter="{StaticResource LockerLabel}">
                                            <Binding Path="Id" />
                                            <Binding Path="DataContext.OwnedIds" RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                        </MultiBinding>
                                    </Button.Content>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
